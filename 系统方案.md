# 多维数据实时分析系统方案

## 一、系统概述

### 1.1 系统名称
**多维数据实时分析系统**

### 1.2 系统定位
本系统是一个面向企业级用户的可视化数据分析平台，提供从数据源接入、数据模型构建、可视化图表设计到仪表盘展示的全流程数据分析解决方案。

### 1.3 核心功能
- 多数据源接入（MySQL、PostgreSQL、Excel）
- 数据模型管理与字段配置
- 可视化图表设计（柱状图、折线图、饼图等）
- 交互式仪表盘构建
- 用户权限管理
- 实时数据预览

---

## 二、技术架构

### 2.1 技术栈

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| **后端框架** | Flask | 2.x |
| **数据库** | SQLite | 3.x |
| **ORM** | SQLAlchemy | 2.x |
| **认证** | Flask-Login | 0.6.x |
| **表单** | Flask-WTF | 1.x |
| **数据处理** | Pandas | 2.x |
| **前端框架** | Bootstrap | 4.6.2 |
| **图表库** | Chart.js | 4.x |
| **交互组件** | jQuery + jQuery UI | 3.6.0 / 1.12.1 |
| **图标** | Font Awesome | 5.15.4 |

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端展示层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 登录注册  │ │ 数据源管理│ │ 数据模型  │ │ 仪表盘设计│       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ 图表配置  │ │ 用户管理  │ │ 个人设置  │                     │
│  └──────────┘ └──────────┘ └──────────┘                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        业务逻辑层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 认证模块  │ │ 数据源模块│ │ 数据模型  │ │ 仪表盘模块│       │
│  │  (auth)  │ │(data_src)│ │(data_mdl)│ │(dashbrd) │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ 图表模块  │ │ 用户模块  │ │ 主模块    │                     │
│  │ (charts) │ │  (users) │ │  (main)  │                     │
│  └──────────┘ └──────────┘ └──────────┘                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据访问层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ SQLAlchemy│ │  Pandas  │ │ 数据库驱动│                     │
│  │   ORM    │ │ 数据处理  │ │MySQL/PostgreSQL│              │
│  └──────────┘ └──────────┘ └──────────┘                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        数据存储层                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                     │
│  │ SQLite   │ │ MySQL    │ │ PostgreSQL│                     │
│  │ (元数据)  │ │ (业务数据)│ │ (业务数据)│                     │
│  └──────────┘ └──────────┘ └──────────┘                     │
│  ┌──────────┐                                               │
│  │ Excel文件│                                               │
│  │ (上传目录)│                                               │
│  └──────────┘                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、数据库设计

### 3.1 实体关系图 (ER图)

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│      User       │       │   DataSource    │       │   DataModel     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ PK id           │◄──────┤ PK id           │◄──────┤ PK id           │
│    username     │   1:N │    name         │   1:N │    name         │
│    email        │       │    type         │       │    table_name   │
│    password_hash│       │    host         │       │    fields (JSON)│
│    is_admin     │       │    port         │       │ FK data_source_id
│    created_at   │       │    username     │       │ FK user_id      │
└─────────────────┘       │    password     │       └────────┬────────┘
         ▲                │    database     │                │
         │                │    file_path    │                │
         │                │ FK user_id      │                │
         │                └─────────────────┘                │
         │                                                   │
         │         ┌─────────────────┐       ┌─────────────────┐
         │         │    Dashboard    │       │      Chart      │
         │         ├─────────────────┤       ├─────────────────┤
         │         │ PK id           │◄──────┤ PK id           │
         │         │    name         │   1:N │    name         │
         │         │    layout (JSON)│       │    type         │
         │         │ FK user_id      │       │    dimensions   │
         └─────────┤    created_at   │       │    metrics      │
              1:N  │    updated_at   │       │    config       │
                   └─────────────────┘       │    position_x   │
                                             │    position_y   │
                                             │    width        │
                                             │    height       │
                                             │ FK dashboard_id │
                                             │ FK data_model_id│
                                             └─────────────────┘
```

### 3.2 数据表结构

#### 3.2.1 用户表 (user)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PK | 主键 |
| username | String(20) | Unique, Not Null | 用户名 |
| email | String(120) | Unique, Not Null | 邮箱 |
| password_hash | String(128) | Not Null | 密码哈希 |
| is_admin | Boolean | Default False | 是否管理员 |

#### 3.2.2 数据源表 (data_source)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PK | 主键 |
| name | String(100) | Not Null | 数据源名称 |
| type | String(20) | Not Null | 类型：mysql/postgresql/excel |
| host | String(255) | | 数据库主机 |
| port | Integer | | 数据库端口 |
| username | String(100) | | 数据库用户名 |
| password | String(255) | | 数据库密码 |
| database | String(100) | | 数据库名 |
| file_path | String(255) | | Excel文件路径 |
| user_id | Integer | FK | 所属用户 |
| created_at | DateTime | Default UTC | 创建时间 |

#### 3.2.3 数据模型表 (data_model)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PK | 主键 |
| name | String(100) | Not Null | 模型名称 |
| data_source_id | Integer | FK, Not Null | 数据源ID |
| table_name | String(100) | Not Null | 表名/工作表名 |
| fields | Text | Not Null | 字段配置(JSON) |
| user_id | Integer | FK, Not Null | 所属用户 |
| created_at | DateTime | Default UTC | 创建时间 |

#### 3.2.4 仪表盘表 (dashboard)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PK | 主键 |
| name | String(100) | Not Null | 仪表盘名称 |
| layout | Text | Not Null | 布局配置(JSON) |
| user_id | Integer | FK, Not Null | 所属用户 |
| created_at | DateTime | Default UTC | 创建时间 |
| updated_at | DateTime | Default UTC | 更新时间 |

#### 3.2.5 图表表 (chart)
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | Integer | PK | 主键 |
| name | String(100) | Not Null | 图表名称 |
| type | String(50) | Not Null | 图表类型 |
| dashboard_id | Integer | FK, Not Null | 仪表盘ID |
| data_model_id | Integer | FK, Not Null | 数据模型ID |
| dimensions | Text | Not Null | 维度配置(JSON) |
| metrics | Text | Not Null | 指标配置(JSON) |
| config | Text | | 其他配置(JSON) |
| position_x | Integer | Not Null | X坐标 |
| position_y | Integer | Not Null | Y坐标 |
| width | Integer | Not Null | 宽度 |
| height | Integer | Not Null | 高度 |
| created_at | DateTime | Default UTC | 创建时间 |
| updated_at | DateTime | Default UTC | 更新时间 |

---

## 四、功能模块设计

### 4.1 认证模块 (auth)

#### 4.1.1 功能清单
- 用户注册
- 用户登录/登出
- 个人设置（修改信息、修改密码）

#### 4.1.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /auth/register | GET/POST | 用户注册 | 公开 |
| /auth/login | GET/POST | 用户登录 | 公开 |
| /auth/logout | GET | 用户登出 | 登录用户 |
| /auth/settings | GET/POST | 个人设置 | 登录用户 |

### 4.2 数据源模块 (data_sources)

#### 4.2.1 功能清单
- 数据源列表
- 添加数据源（MySQL、PostgreSQL、Excel）
- 编辑数据源
- 删除数据源
- 测试连接
- 获取表名/工作表列表

#### 4.2.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /data_sources/ | GET | 数据源列表 | 登录用户 |
| /data_sources/add | GET/POST | 添加数据源 | 登录用户 |
| /data_sources/<id>/edit | GET/POST | 编辑数据源 | 登录用户 |
| /data_sources/<id>/delete | POST | 删除数据源 | 登录用户 |
| /data_sources/get_tables | POST | 获取表名列表 | 登录用户 |

### 4.3 数据模型模块 (data_models)

#### 4.3.1 功能清单
- 数据模型列表
- 添加数据模型
- 编辑数据模型
- 删除数据模型
- 获取字段列表
- 数据预览

#### 4.3.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /data_models/ | GET | 模型列表 | 登录用户 |
| /data_models/add | GET/POST | 添加模型 | 登录用户 |
| /data_models/<id>/edit | GET/POST | 编辑模型 | 登录用户 |
| /data_models/<id>/delete | POST | 删除模型 | 登录用户 |
| /data_models/get_fields | POST | 获取字段 | 登录用户 |
| /data_models/preview_data | POST | 数据预览 | 登录用户 |

### 4.4 仪表盘模块 (dashboards)

#### 4.4.1 功能清单
- 仪表盘列表
- 创建仪表盘
- 编辑仪表盘（拖拽布局）
- 删除仪表盘
- 预览模式
- 自动保存布局

#### 4.4.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /dashboards/ | GET | 仪表盘列表 | 登录用户 |
| /dashboards/add | GET/POST | 创建仪表盘 | 登录用户 |
| /dashboards/<id>/edit | GET/POST | 编辑仪表盘 | 登录用户 |
| /dashboards/<id>/delete | POST | 删除仪表盘 | 登录用户 |
| /dashboards/<id>/save_layout | POST | 保存布局 | 登录用户 |
| /dashboards/<id>/get_charts | GET | 获取图表 | 登录用户 |

### 4.5 图表模块 (charts)

#### 4.5.1 功能清单
- 添加图表
- 编辑图表
- 删除图表
- 图表预览
- 图表数据获取

#### 4.5.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /charts/add/<dashboard_id> | GET/POST | 添加图表 | 登录用户 |
| /charts/<id>/edit | GET/POST | 编辑图表 | 登录用户 |
| /charts/<id>/delete | POST | 删除图表 | 登录用户 |
| /charts/<id>/data | GET | 获取图表数据 | 登录用户 |
| /charts/<id>/preview | POST | 预览图表 | 登录用户 |

### 4.6 用户管理模块 (users)

#### 4.6.1 功能清单
- 用户列表（管理员）
- 添加用户（管理员）
- 编辑用户（管理员）
- 删除用户（管理员）

#### 4.6.2 路由设计
| 路由 | 方法 | 功能 | 权限 |
|------|------|------|------|
| /users/ | GET | 用户列表 | 管理员 |
| /users/add | GET/POST | 添加用户 | 管理员 |
| /users/<id>/edit | GET/POST | 编辑用户 | 管理员 |
| /users/<id>/delete | POST | 删除用户 | 管理员 |

---

## 五、前端设计

### 5.1 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  Logo                      面包屑导航        [用户下拉菜单]   │
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│  首页    │                                                  │
│  数据源  │              主内容区域                           │
│  数据模型 │                                                  │
│  仪表盘  │                                                  │
│  图表    │                                                  │
│  用户管理│                                                  │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

### 5.2 响应式布局
- 桌面端：左侧固定侧边栏 + 右侧主内容区
- 平板/移动端：可折叠侧边栏

### 5.3 静态资源结构
```
static/
├── vendor/              # 第三方库
│   ├── bootstrap/       # Bootstrap 4.6.2
│   ├── fontawesome/     # Font Awesome 5.15.4
│   ├── jquery/          # jQuery 3.6.0
│   ├── jqueryui/        # jQuery UI 1.12.1
│   └── chartjs/         # Chart.js 4.x
├── uploads/             # 上传文件目录
└── css/                 # 自定义样式
```

---

## 六、安全设计

### 6.1 认证机制
- 基于 Session 的用户认证
- Flask-Login 管理用户会话
- 密码使用 Werkzeug 进行哈希存储

### 6.2 权限控制
- 登录装饰器保护敏感路由
- 管理员装饰器保护管理功能
- 用户只能访问自己的数据

### 6.3 数据安全
- SQL 注入防护：使用 SQLAlchemy ORM
- XSS 防护：模板自动转义
- CSRF 防护：Flask-WTF 表单保护

---

## 七、部署方案

### 7.1 环境要求
- Python 3.8+
- pip 包管理器
- 虚拟环境（推荐）

### 7.2 安装步骤
```bash
# 1. 创建虚拟环境
python -m venv venv

# 2. 激活虚拟环境
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 3. 安装依赖
pip install -r requirements.txt

# 4. 初始化数据库
python -c "from app import create_app, db; app = create_app(); app.app_context().push(); db.create_all()"

# 5. 运行应用
python run.py
```

### 7.3 生产环境建议
- 使用 Gunicorn 或 uWSGI 作为 WSGI 服务器
- 使用 Nginx 作为反向代理
- 使用 MySQL/PostgreSQL 替代 SQLite
- 配置 HTTPS
- 设置环境变量（SECRET_KEY、DATABASE_URL 等）

---

## 八、扩展规划

### 8.1 近期扩展
- [ ] 更多图表类型（散点图、雷达图、漏斗图等）
- [ ] 数据筛选和过滤功能
- [ ] 图表联动功能
- [ ] 数据导出（PDF、图片）

### 8.2 中期扩展
- [ ] 支持更多数据源（Oracle、SQL Server、MongoDB）
- [ ] 数据定时刷新
- [ ] 仪表盘分享功能
- [ ] 移动端适配优化

### 8.3 远期扩展
- [ ] 数据告警功能
- [ ] 多租户支持
- [ ] 数据血缘追踪
- [ ] AI 智能分析建议

---

## 九、项目结构

```
PythonReprot/
├── app/                          # 应用主目录
│   ├── __init__.py              # 应用工厂函数
│   ├── auth/                    # 认证模块
│   │   ├── __init__.py
│   │   ├── forms.py            # 表单定义
│   │   ├── models.py           # 用户模型
│   │   └── routes.py           # 路由处理
│   ├── charts/                  # 图表模块
│   │   ├── __init__.py
│   │   ├── forms.py
│   │   ├── models.py
│   │   └── routes.py
│   ├── dashboards/              # 仪表盘模块
│   │   ├── __init__.py
│   │   ├── models.py
│   │   └── routes.py
│   ├── data_models/             # 数据模型模块
│   │   ├── __init__.py
│   │   ├── forms.py
│   │   ├── models.py
│   │   └── routes.py
│   ├── data_sources/            # 数据源模块
│   │   ├── __init__.py
│   │   ├── forms.py
│   │   ├── models.py
│   │   └── routes.py
│   ├── main/                    # 主模块
│   │   ├── __init__.py
│   │   └── routes.py
│   ├── users/                   # 用户管理模块
│   │   ├── __init__.py
│   │   └── routes.py
│   ├── static/                  # 静态资源
│   │   ├── vendor/             # 第三方库
│   │   └── uploads/            # 上传文件
│   └── templates/              # 模板文件
│       ├── auth/               # 认证模板
│       ├── charts/             # 图表模板
│       ├── dashboards/         # 仪表盘模板
│       ├── data_models/        # 数据模型模板
│       ├── data_sources/       # 数据源模板
│       ├── main/               # 主页模板
│       ├── users/              # 用户管理模板
│       └── base.html           # 基础模板
├── config.py                   # 配置文件
├── requirements.txt            # 依赖列表
├── run.py                      # 启动脚本
└── 系统方案.md                # 本文档
```

---

## 十、总结

本系统采用经典的 Flask 分层架构，模块化设计使得各功能组件职责清晰、易于维护。通过 Blueprint 实现模块解耦，SQLAlchemy 处理数据持久化，前端采用成熟的 Bootstrap + jQuery 技术栈，配合 Chart.js 实现数据可视化。

系统支持多种数据源接入，提供从数据接入到可视化展示的一站式解决方案，适用于企业内部数据分析、报表展示等场景。
